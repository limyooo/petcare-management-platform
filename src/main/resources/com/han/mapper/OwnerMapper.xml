<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.han.mapper.OwnerMapper">

    <insert id="insert">
        INSERT INTO pet_owner(name, phone, gender, email, address, create_time, update_time)
        VALUES (#{name}, #{phone}, #{gender}, #{email}, #{address}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateByOwnerId">
        UPDATE pet_owner
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteByOwnerIds">
        DELETE FROM pet_owner WHERE id IN
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="list" resultType="com.han.pojo.Owner">
        SELECT * FROM pet_owner
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="gender != null">
                AND gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                AND create_time BETWEEN #{begin} AND #{end}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>

    <select id="get" resultType="com.han.pojo.Owner">
        SELECT * FROM pet_owner WHERE id = #{id}
    </select>

    <select id="countByGender" resultType="java.util.Map">
        SELECT
        (CASE WHEN gender = 1 THEN 'Male'
        WHEN gender = 2 THEN 'Female'
        ELSE 'Unknown' END) AS name,
        COUNT(*) AS num
        FROM pet_owner
        GROUP BY gender
    </select>

    <select id="countByAddress" resultType="java.util.Map">
        SELECT
        TRIM(SUBSTRING_INDEX(address, ',', -1)) AS name,
        COUNT(*) AS num
        FROM pet_owner
        WHERE address IS NOT NULL
        GROUP BY TRIM(SUBSTRING_INDEX(address, ',', -1))
        ORDER BY num DESC
    </select>

</mapper>